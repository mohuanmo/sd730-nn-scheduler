# SD730 Neural Scheduler v2.0 - 搜索改进报告

## 搜索发现的关键信息

### 1. big.LITTLE 调度器迁移开销（llama.cpp 经验）
- Android 的默认调度器会频繁迁移线程 between big/LITTLE，导致 L1/L2 Cache Flush
- 固定线程到 big 核可消除抖动，但需要在性能和功耗间平衡
- **我们的 Thread Pin Engine 方向正确**，但 NN 应该学会"何时固定、何时释放"

### 2. CoreTaskOptimizer（Magisk 模块最佳实践）
- 使用 **C++ 原生二进制** 直接调用 syscalls，而非 shell 脚本
- 线程级精度：枚举 /proc/<pid>/task 下的每个线程
- 一次启动后退出，无守护进程，零常驻内存
- **改进建议**：当前 awk 推理已足够轻量（<1μs），但可考虑提供可选的 C++ 推理二进制

### 3. TinyML 优化技术
- 量化（Quantization）：将 float32 转为 int8，减少 75% 模型大小和计算量
- 剪枝（Pruning）：移除冗余参数
- 内存调度：优化数据布局减少内存访问
- **改进建议**：177 参数已极小，但可尝试 8-bit 定点数 awk 实现

### 4. 热节流与核心分配冲突（XDA）
- 多个模块同时修改 CPU/GPU 调度时会产生冲突
- big 集群的硬编码温度限制可能导致小集群先节流
- **改进建议**：我们的温度兜底（>70°C 回退规则）是安全的，但可更平滑

### 5. 元学习与在线优化（TinyML 工业综述）
- 设备端自适应学习可减少 50% 能耗、60% 通信开销
- 强化学习调度器可提升 38% 能效
- **改进建议**：当前"夜间训练"就是在线优化，但可引入 EMA 增量更新而非全量重训

---

## 当前方案的不合理之处与改进路线

### 不合理 1：screen_on 检测不够准确
**现状**：通过前台 app 是否为空/系统进程间接推断屏幕状态。
**问题**：锁屏但有通知、AOD（Always-On Display）时会误判为 screen_on=0。
**改进**：增加 dumpsys power 检测作为辅助验证（开销较大，可每 5 秒采样一次）。
**优先级**：低（不影响核心逻辑）

### 不合理 2：缺少"刚切前台"预热特征
**现状**：模型不知道 app 是刚打开还是已运行很久。
**问题**：刚切前台的 app 应该允许更激进的 big 核预热（避免启动卡顿）。
**改进**：增加 `seconds_since_foreground` 特征，< 3 秒时 k 自动上浮 0.3。
**优先级**：高（直接影响用户体验）

### 不合理 3：训练评分函数未考虑迁移开销
**现状**：评分只考虑瞬时状态，未考虑把线程从 little 移到 big 的上下文切换成本。
**问题**：频繁切换会导致 Cache 失效，反而降低性能。
**改进**：评分函数增加"状态稳定性"奖励（连续 3 秒相同决策 +0.1 分）。
**优先级**：中

### 不合理 4：没有 app 类型先验
**现状**：所有 app 共享同一个模型，没有区分游戏/视频/浏览器。
**问题**：游戏类 app 的 optimal k 分布和浏览器完全不同。
**改进**：
- 方案 A：增加 app 类别 one-hot 编码（输入维度 +5）
- 方案 B：为每个 app 维护独立的 EMA k 偏移量（轻量）
**优先级**：中

### 不合理 5：模型输出单一 k，缺少弹性预算感知
**现状**：k 影响所有线程的阈值，但 Thread Pin Engine 的弹性预算（TP_BASE_CAP/TP_MAX_CAP）是独立硬编码的。
**问题**：k=2.0 时大量线程达到 hot 阈值，但弹性预算只允许 2-3 个线程上 big 核，导致"有资格但上不了"。
**改进**：让 NN 同时输出 `[k, cap_ratio]`，动态调整弹性预算上限。
**优先级**：高（否则 k 的效果被预算卡死）

### 不合理 6：夜间训练窗口固定
**现状**：默认 1-4 点训练，学习用户习惯后调整。
**问题**：用户可能 2 点才睡，1 点开始训练会干扰使用。
**改进**：增加"充电 + 屏幕关闭 + 无交互"作为训练触发条件，而非固定时间窗口。
**优先级**：高

### 不合理 7：collector 数据没有区分"充电中采集"和"电池采集"
**现状**：所有数据混在一起训练。
**问题**：充电时的 optimal k 和不充电时完全不同。
**改进**：训练时按 charging 状态分组，分别学习两个子模型，或增加 charging 交互项。
**优先级**：中

---

## 下一步优先级

```
P0（立即）：
  - 增加 "seconds_since_foreground" 特征
  - 让 NN 同时输出 cap_ratio，解除弹性预算限制
  - 训练触发改为"充电+深度待机"而非固定时间

P1（验证后）：
  - 引入 app 类型先验（EMA 偏移量）
  - 评分函数增加迁移开销惩罚
  - 8-bit 量化实验

P2（长期）：
  - C++ 推理二进制（NDK 编译）
  - 联邦学习：多台设备共享模型更新
  - 强化学习替代监督学习（在线策略梯度）
```
