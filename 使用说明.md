# SD730 Neural Scheduler v3.2 — 使用说明（含合并 CLI）

> 本包是 `sd730-scheduler`（v1.5.4）的 **神经网络增强组件**，提供：神经网络线程级调度
> （场景编码 + 线程打分）、数据采集、双引擎训练（numpy / 纯 Python）、awk 推理、自动打补丁。
>
> **v3.2 变更**：V2 场景级模型（k/cap）已移除，只保留神经网络（线程级重排）模型；
> 默认启用模型 + 默认开启自动训练。
>
> **NN 命令已合并进主 CLI**：用 `sd730-scheduler --nn-*` 统一控制，无需再单独调脚本。

---

## 〇、模型参数（神经网络模型，657 参数）

| 模块 | 层 | 形状 | 参数 |
|---|---|---|---|
| 场景编码器 E | W1(+b1) / Wk(+bk) / Ws(+bs) | 25×10 / 10×2 / 10×10 | 392 |
| 线程打分器 T | W1(+b1) / W2(+b2) | 31×8 / 8×1 | 265 |
| 合计 | | | **657** |

编码器输入 25 维: 聚合10 + smooth(帧时间方差流畅度)1 + 应用类别6 + 包名hash8
打分器输入 31 维: 场景向量10 + 线程特征21(cpu% + 类型onehot12 + 线程名hash8)

### 训练超参
| 项 | train_thread.py (numpy) | train_thread_pure.py (纯Python) |
|---|---|---|
| 优化 | batch SGD | batch SGD |
| epochs / lr | 200 / 0.05 | 60 / 0.05 |
| 标签 | 帧时间方差反馈 | 帧时间方差反馈 |
| 样本上限 | 12000 | 3000 |

---
## 一、合并后的 CLI 命令总表

### 原模块命令（保持不变）

| 命令 | 作用 |
|---|---|
| `sd730-scheduler --mode <powersave\|balanced\|performance\|ultra>` | 切换性能模式 |
| `sd730-scheduler --status` | 查看状态/频率/GPU/线程钉扎/预测 |
| `sd730-scheduler --stats` / `--prediction-stats` | 学习库/预测库统计 |
| `sd730-scheduler --reset-learning` / `--reset-prediction` | 重置学习库/预测库 |
| `sd730-scheduler --daemon` | 后台守护运行学习循环 |
| `sd730-scheduler --affinity <包名>` | 查看应用推荐亲和掩码 |
| `sd730-scheduler --threads [包名]` | 实时线程 CPU% 与掩码 |
| `sd730-scheduler --gpu-status` | GPU 自适应调度状态 |
| `sd730-scheduler --tpin-*` | 线程钉扎引擎控制 |
| `sd730-scheduler --scene <mode>` | Scene 集成 |
| `sd730-scheduler --help` | 帮助 |

### 新增 NN 命令（本包合并）

| 命令 | 作用 | 对应底层脚本 |
|---|---|---|
| `sd730-scheduler --nn-status` | **查看模型学习状态**（模型/数据/自动训练/实时线程分数） | — |
| `sd730-scheduler --nn-train` | **手动触发模型训练**（自动选 numpy/纯 Python 引擎） | bin/train.sh |
| `sd730-scheduler --nn-train-now` | **判断后立即强制训练**（`--force`，跳过样本量门槛，温度仅保留 85°C 绝对上限） | bin/train.sh --force |
| `sd730-scheduler --nn-test` | **模型运行测试**（完整性 + 推理范围校验） | — |
| `sd730-scheduler --nn-data` | 训练数据统计（traw 行数/天数/大小/采集器状态） | — |
| `sd730-scheduler --nn-enable` / `--nn-disable` | 启用/禁用神经调度器（总开关） | 改 nn.conf |
| `sd730-scheduler --nn-v3-enable` / `--nn-v3-disable` | 启用/禁用神经网络模型线程重排（**默认启用**） | 改 v3.conf |
| `sd730-scheduler --nn-v3-status` | `--nn-status` 的兼容别名 | — |

---

## 二、部署与打补丁

```sh
# 1. 复制组件到已安装的模块目录
cp -r bin common config system train.py train_pure.py patch_nn.py \
    /data/adb/modules/sd730-scheduler/
# 注意: system/bin/sd730-scheduler 覆盖安装到模块的 system/bin/ 后需重启生效
# (或直接替换 /system/bin/sd730-scheduler, 重新挂载可写)

# 2. 打补丁：注入 NN 逻辑到 functions.sh（自动备份 .bak, 幂等）
sh patch_nn.sh
# 输出: [+] functions.sh patched successfully (v2.1)

# 3. 验证补丁
grep -c "NN_CAP" /data/adb/modules/sd730-scheduler/common/functions.sh   # >= 2

# 4. 回滚
cp /data/adb/modules/sd730-scheduler/common/functions.sh.bak \
   /data/adb/modules/sd730-scheduler/common/functions.sh
```

---

## 三、如何查看模型学习状态

```sh
sd730-scheduler --nn-status
```

输出示例与解读：

```
=== Neural Network Model Status (神经网络模型状态) ===
Enabled: true (总开关) | model reorder: true (config/v3.conf)   ← 模型已启用
Auto-train: true (config/v3.conf, 训练窗口内自动训练)           ← 自动训练开
Train window: 1:00-4:00, max temp 55C
Temperature limit: 70C (above -> force rule-only)
Data retention: 5 days (collector auto-rotates)

=== Model ===
Model: EXISTS (encoder 25->10 + scorer 10+21->8->1, 657 params) ← 模型存在
  encoder lines: 48 (expect 48)
  scorer lines:  41 (expect 41)
  last trained: 2026-08-16 10:32:12                              ← 上次训练时间

=== Training Data (.traw) ===
traw files: 1, total rows: 576 (need >=300 to train)             ← 样本量
Training readiness: READY                                        ← 是否可训练

=== Live Thread Scores ===
T1|0.82|45.1                                                     ← 当前线程实时打分
```

补充查看：
```sh
sd730-scheduler --nn-status        # 采集器是否运行 + 数据规模
cat /data/adb/modules/sd730-scheduler/model/mlp_v3_enc.txt | head -1   # 25 列
cat /data/adb/modules/sd730-scheduler/model/mlp_v3_scr.txt | wc -l     # 41 行
```

---

## 四、如何手动触发训练

```sh
sd730-scheduler --nn-train
```

- 自动探测 Python：PATH → py2droid(/system/usr/bin) → Termux
- 自动选引擎：有 numpy → train_thread.py（快，200 epochs）；无 numpy → train_thread_pure.py（纯标准库，60 epochs）
- 内置门槛：traw 样本 <300 跳过（--force 可绕过）
- 训练完成输出：模型路径（mlp_v3_enc.txt + mlp_v3_scr.txt，657 参数）

**判断后立即训练**（例如刚观察到前台游戏卡顿，想立刻让模型学习当前状态）：
```sh
sd730-scheduler --nn-train-now    # 等效: sh bin/train.sh --force, 跳过样本量门槛
```

**强制模式（--force）语义**：
- 跳过 样本量门槛（≥300），样本不足时**照常训练**但打印明显警告（样本过少模型易过拟合/质量差，建议先让采集器多跑一阵）；
- 温度：训练前检测，超过 `nn_train_max_temp`（默认 55°C）非强制会跳过；强制模式下超限只警告继续，**绝对超过 85°C 才中止**（防物理损坏）；
- **终端实时显示训练日志**：`train.sh` 用 `python3 -u` 无缓冲调用，Epoch loss 逐行实时打印（修复了管道缓冲导致日志攒到结束才输出的问题）。

**自动训练（默认开启）**：每天训练窗口内（`nn_train_start_hour:00`~`nn_train_end_hour:00`，默认 1:00-4:00）自动训练，样本 ≥300 才训，成功当天不重复，失败自动重试。开关：`config/v3.conf` 的 `v3_auto_train`（默认 true）。

温度传感器修复：旧版把 `/sys/class/thermal/thermal_zone*/temp` 所有原始值取最大再 ÷1000，会误把非温度传感器当成高温（例如本机 `lmh-dcvs-00/01` 恒报 75000 → 误判 75°C 而错误阻断训练）。新版只统计真正的温度传感器（type 以 `-tz`/`-usr` 结尾、含 `therm`、或 `battery`/`bms`），自动归一化毫摄氏度/十分之一度/摄氏度单位，无效时回退读电池温度。

```sh
# 指定引擎直接跑（调试用）
python3 /data/adb/modules/sd730-scheduler/train_thread.py        # numpy 版
python3 /data/adb/modules/sd730-scheduler/train_thread_pure.py   # 纯 Python 版
```

---

## 五、如何测试模型能否正常运行

```sh
sd730-scheduler --nn-test
```

输出示例：

```
=== Neural Network Model Test (模型测试) ===
[1] encoder: 48 lines (expect 48 = 25W1+1b1+10Wk+1bk+10Ws+1bs)
    scorer:  41 lines (expect 41 = 31W1+1b1+8W2+1b2)
    ✓ layout OK
[2] Live thread scoring:
    T1|0.82|45.1
    ✓✓✓ MODEL TEST PASSED - inference pipeline healthy
```

- 模型缺失 → 提示 `--nn-train-now`
- 布局行数不对 → 判定失败并提示重训（模型文件可能损坏）

---

## 五-b、训练数据量估算与抽样策略

**数据量估算**（traw 每 5 秒采样一次，仅前台有 app 时采集）：

| 使用强度 | 每天条数 | 5 天原始数据 | 训练实际用（抽样上限）|
|---|---|---|---|
| 轻度（4h/天）| ~2,900 条 | ~14,500 条 | 3,000 条（纯Python版）|
| 普通（6h/天）| ~4,300 条 | ~21,500 条 | 3,000 条 |
| 中度（8h/天）| ~5,800 条 | ~29,000 条 | 3,000 条 |
| 重度（10h/天）| ~7,200 条 | ~36,000 条 | 3,000 条 |

模型 657 参数，需求经验值 6500~20000 条；训练门槛 300 条可快速启动（少量样本先跑通流程），
数据越充分模型越准。numpy 版抽样上限 12000，纯 Python 版 3000（控制手机训练时长）。

**时间衰减分层抽样**：
- 问题：均匀抽样时若最近 1-2 天数据量就攒够上限，前面几天的数据会被完全抽不到（模型"失忆"）
- 方案：按天配额，每天至少保留代表性样本；权重随天数轻微衰减（最近 1.0，每往前一天 ×0.9）

## 六、数据采集

```sh
# 启动采集器（常驻 1s 采样）
sh /data/adb/modules/sd730-scheduler/bin/collector.sh &

# 验证
wc -l /data/adb/modules/sd730-scheduler/data/collector/$(date +%Y%m%d).traw
cat /data/adb/modules/sd730-scheduler/data/collector/.coll_state   # 6字段快照
```

> **v3.1 起采集器开机自动启动，无需手动执行！** 由以下三处兜底拉起
> （`bin/ensure_collector.sh`，幂等，已运行则跳过）：
> 1. `post-fs-data.sh` 最前面（early 阶段）
> 2. `service.sh` 最前面 + 主循环 30 秒周期 watchdog
> 3. `common/functions.sh` 被 source 时的自愈钩子（覆盖旧版 service.sh）
>
> 手动启动仅用于调试。排查时看采集器日志：
> `cat /data/local/tmp/sd730-collector.log`（含 `ensure: stage=xxx starting collector` 记录）。
> `sd730-scheduler --nn-data` 可查看采集器运行状态。

保留天数：`config/nn.conf` 的 `nn_data_keep_days`（默认 5，范围 1-30），collector 每 1 秒轮询自动删除过期 `.traw`。

---

## 七、配置文件

### config/nn.conf（基础配置）

| 配置 | 默认 | 说明 |
|---|---|---|
| `nn_enabled` | `true` | 总开关（可用 --nn-enable/--nn-disable） |
| `nn_temp_limit` | `70` | 温度兜底（°C），超过强制回退规则调度 |
| `nn_data_keep_days` | `5` | 采集数据保留天数 |
| `nn_train_start_hour` / `nn_train_end_hour` | `1` / `4` | 训练窗口 |
| `nn_train_max_temp` | `55` | **训练温度上限（°C）**：训练前 + 训练中巡检，超限跳过/中止 |

### config/v3.conf（神经网络模型）

| 配置 | 默认 | 说明 |
|---|---|---|
| `v3_enabled` | `true` | 模型启用（**默认开启**，--nn-v3-enable/--nn-v3-disable 切换） |
| `v3_auto_train` | `true` | 自动训练（**默认开启**，窗口内样本≥300 自动训） |

### 训练安全门控

训练不再无条件执行，一道安全门：

```sh
# 门: 温度上限 (nn_train_max_temp, 默认 55°C)
#     非强制: 训练前检测 + 训练中巡检, 超限立即停止且保留旧模型
#     强制(--force): 超限仅警告继续; 绝对超过 85°C 才中止 (防物理损坏)
# 可调: config/nn.conf 的 nn_train_max_temp (30-90°C)
```

温度传感器读取 (v3.1.1)：只统计真正的温度传感器 zone，自动适配不同单位，并过滤
`lmh-dcvs`/`ibat`/`vbat`/`bcl`/`soc`/`*-step`/`*-lowf` 等非温度节点（旧版会把
`lmh-dcvs` 恒报的 75000 误判成 75°C 而错误阻断训练）。传感器全部不可用时不阻断训练；
超限时输出明确原因并退出码 1。

---

## 八、测试套件

```sh
cd /data/adb/modules/sd730-scheduler/test
python3 test_nn_scheduler.py       # v1.6 回归 60 用例
python3 test_model_learning.py     # 模型学习能力
python3 test_v2_1_pipeline.py      # 端到端管线 33 项
python3 test_pure_training.py      # 纯 Python 引擎 17 项
sh test_collector_sim.sh           # 采集器模拟 7 项
sh simulate_env_test.sh            # 真机环境模拟 15 项
```

---

## 九、上线流程（推荐）

```sh
# 1. 采集 1-2 天数据 (模型默认启用 + 自动训练默认开启, 基本不用管)
# 2. 查看学习状态 (数据量 / 模型 / 自动训练)
sd730-scheduler --nn-status
# 3. 数据够 300 条后手动训练 (或等凌晨窗口自动训练)
sd730-scheduler --nn-train-now
# 4. 测试模型
sd730-scheduler --nn-test
# 5. 确认生效: --nn-status 显示 Model: EXISTS 且 model reorder: true
```

---

## 十、故障排查

| 现象 | 原因 | 处理 |
|---|---|---|
| `--nn-status` 显示模型 MISSING | 未训练 | `sd730-scheduler --nn-train-now` |
| `--nn-test` 判定失败 | 模型损坏/布局不对 | 重训；或确认 enc 48 行 / scr 41 行 |
| 训练报 numpy ImportError | 无 numpy | 自动切 train_thread_pure.py（纯标准库）；或 pip 装 numpy |
| `--nn-train` 无反应 | traw 样本 <300 或 Python 缺失 | `--nn-status` 看 Training readiness |
| `.traw` 不增长 | 采集器未运行 | `sh bin/collector.sh &`，亮屏使用 |
| 模型存在但线程不重排 | v3_enabled=false | `sd730-scheduler --nn-v3-enable` |

