# SD730 Neural Scheduler v2.1.4 — 使用说明（含合并 CLI）

> 本包是 `sd730-scheduler`（v1.5.4）的 **NN 增强组件**，提供：神经网络调度（k/cap 双输出）、
> 数据采集、双引擎训练（numpy / 纯 Python）、awk 推理、自动打补丁。
>
> **NN 命令已合并进主 CLI**：用 `sd730-scheduler --nn-*` 统一控制，无需再单独调脚本。

---

## 〇、模型参数（v2 + v3）

### v2 模型（场景级 k/cap，model/mlp_weights.txt，210 参数）
| 层 | 形状 | 参数 |
|---|---|---|
| W1 (+b1) | 10×16 (+16) | 176 |
| W2 (+b2) | 16×2 (+2) | 34 |
| 合计 | | **210** |

输入 10 维: cpu/gpu/temp/batt/chg/screen/threads/mem/hour/fg_duration
输出: k∈[0.5,2.0]（激进度）, cap∈[0.5,1.5]（弹性预算）

### v3 模型（线程级双模块，mlp_v3_enc.txt + mlp_v3_scr.txt，657 参数）
| 模块 | 层 | 形状 | 参数 |
|---|---|---|---|
| 场景编码器 E | W1(+b1) / Wk(+bk) / Ws(+bs) | 25×10 / 10×2 / 10×10 | 392 |
| 线程打分器 T | W1(+b1) / W2(+b2) | 31×8 / 8×1 | 265 |
| 合计 | | | **657** |

编码器输入 25 维: 聚合10 + smooth(帧时间方差流畅度)1 + 应用类别6 + 包名hash8
打分器输入 31 维: 场景向量10 + 线程特征21(cpu% + 类型onehot12 + 线程名hash8)

### 训练超参
| 项 | train.py | train_pure.py | train_thread.py |
|---|---|---|---|
| 优化 | batch SGD | mini-batch SGD | batch SGD |
| epochs / lr | 300 / 0.05 | 60 / 0.05 | 200 / 0.05 |
| 标签 | 启发式网格 | 同左 | 帧时间方差反馈 |

---
## 一、合并后的 CLI 命令总表

### 原模块命令（保持不变）

| 命令 | 作用 |
|---|---|
| `sd730-scheduler --mode <powersave\|balanced\|performance\|ultra>` | 切换性能模式 |
| `sd730-scheduler --status` | 查看状态/频率/GPU/线程钉扎/预测 |
| `sd730-scheduler --stats` / `--prediction-stats` | 学习库/预测库统计 |
| `sd730-scheduler --reset-learning` / `--reset-prediction` | 重置学习库/预测库 |
| `sd730-scheduler --daemon` | 后台守护运行学习循环 |
| `sd730-scheduler --affinity <包名>` | 查看应用推荐亲和掩码 |
| `sd730-scheduler --threads [包名]` | 实时线程 CPU% 与掩码 |
| `sd730-scheduler --gpu-status` | GPU 自适应调度状态 |
| `sd730-scheduler --tpin-*` | 线程钉扎引擎控制 |
| `sd730-scheduler --scene <mode>` | Scene 集成 |
| `sd730-scheduler --help` | 帮助 |

### 新增 NN 命令（本包合并）

| 命令 | 作用 | 对应底层脚本 |
|---|---|---|
| `sd730-scheduler --nn-status` | **查看模型学习状态**（模型/数据/alpha/实时推理） | bin/nn_infer.sh |
| `sd730-scheduler --nn-train` | **手动触发模型训练**（自动选 numpy/纯 Python 引擎） | bin/train.sh |
| `sd730-scheduler --nn-train-now [--v3]` | **判断后立即强制训练**（`--force`：跳过 待机/样本量 门槛，温度仅保留 85°C 绝对上限；`--v3` 改为训练 v3 线程级模型） | bin/train.sh --force [--v3] |
| `sd730-scheduler --nn-infer` | 手动推理一次（输出 `k cap_ratio`） | bin/nn_infer.sh |
| `sd730-scheduler --nn-test` | **模型运行测试**（完整性 + 推理范围校验） | — |
| `sd730-scheduler --nn-data` | 采集数据统计（行数/天数/大小/采集器运行状态） | — |
| `sd730-scheduler --nn-alpha <0.0-1.0>` | 设置 NN 混合比例（建议 ≤0.5） | 改 nn.conf |
| `sd730-scheduler --nn-enable` / `--nn-disable` | 启用/禁用 NN | 改 nn.conf |
| `sd730-scheduler --nn-warmup-test` | 前台预热检测状态 | — |

---

## 二、部署与打补丁

```sh
# 1. 复制组件到已安装的模块目录
cp -r bin common config system train.py train_pure.py patch_nn.py \
    /data/adb/modules/sd730-scheduler/
# 注意: system/bin/sd730-scheduler 覆盖安装到模块的 system/bin/ 后需重启生效
# (或直接替换 /system/bin/sd730-scheduler, 重新挂载可写)

# 2. 打补丁：注入 NN 逻辑到 functions.sh（自动备份 .bak, 幂等）
sh patch_nn.sh
# 输出: [+] functions.sh patched successfully (v2.1)

# 3. 验证补丁
grep -c "NN_CAP" /data/adb/modules/sd730-scheduler/common/functions.sh   # >= 2

# 4. 回滚
cp /data/adb/modules/sd730-scheduler/common/functions.sh.bak \
   /data/adb/modules/sd730-scheduler/common/functions.sh
```

---

## 三、如何查看模型学习状态

```sh
sd730-scheduler --nn-status
```

输出示例与解读：

```
=== Neural Scheduler Status ===
Enabled: true                                  ← NN 总开关
Alpha (NN mix ratio): 0.3                      ← 当前混合比例
Temperature limit: 70C (above -> force rule-only)
Data retention: 3 days (collector auto-rotates)

=== Model ===
Model: EXISTS (architecture: 10 16 2)          ← 模型存在, 架构正确
Last trained: 2026-08-15 02:31:05              ← 上次训练时间

=== Training Data ===
Data files: 3 (dates 20260813 .. 20260815)     ← 覆盖天数
Total samples: 12345 (need >=500 to train)     ← 样本量
Training readiness: READY                      ← 是否可训练

=== Live Inference ===
nn_infer.sh -> 1.4210 0.6000  (k cap_ratio)    ← 当前状态的实时推理
```

补充查看：
```sh
sd730-scheduler --nn-data        # 采集器是否运行 + 数据规模
cat /data/adb/modules/sd730-scheduler/model/mlp_weights.txt | head -1   # 10 16 2
```

---

## 四、如何手动触发训练

```sh
sd730-scheduler --nn-train
```

- 自动探测 Python：PATH → py2droid(/system/usr/bin) → Termux
- 自动选引擎：有 numpy → train.py（快）；无 numpy → train_pure.py（纯标准库）
- 内置门槛：样本 <500 跳过；非深度待机（未充电/亮屏/有前台）会提示但不强制
- 训练完成输出：模型路径 + 参数数（210 floats）

**判断后立即训练**（例如刚观察到前台游戏卡顿，想立刻让模型学习当前状态）：
```sh
sd730-scheduler --nn-train-now    # 等效: sh bin/train.sh --force
sd730-scheduler --nn-train-now --v3   # 强制训练 v3 线程级模型 (train_thread.py, 需 numpy)
```

**强制模式（--force）语义（v3.1.1）**：
- 跳过 深度待机（屏幕/前台/充电）门槛，亮屏、未充电也能训练；
- 跳过 样本量门槛（v2 需 ≥500、v3 需 ≥300），样本不足时**照常训练**但打印明显警告（样本过少模型易过拟合/质量差，建议先让采集器多跑一阵）；
- 温度：常规上限 `nn_train_max_temp`（默认 55°C）仅拦截非强制训练；强制模式下超限只警告继续，**绝对超过 85°C 才中止**（防物理损坏）；
- **终端实时显示训练日志**：`train.sh` 用 `python3 -u` 无缓冲调用，Epoch loss 逐行实时打印（修复了管道缓冲导致日志攒到结束才输出的问题）。

温度传感器修复（v3.1.1）：旧版把 `/sys/class/thermal/thermal_zone*/temp` 所有原始值取最大再 ÷1000，会误把非温度传感器当成高温（例如本机 `lmh-dcvs-00/01` 恒报 75000 → 误判 75°C 而错误阻断训练）。新版只统计真正的温度传感器（type 以 `-tz`/`-usr` 结尾、含 `therm`、或 `battery`/`bms`），自动归一化毫摄氏度/十分之一度/摄氏度单位，无效时回退读电池温度。

```sh
# 指定引擎直接跑（调试用）
python3 /data/adb/modules/sd730-scheduler/train.py       # numpy 版
python3 /data/adb/modules/sd730-scheduler/train_pure.py  # 纯 Python 版
```

---

## 五、如何测试模型能否正常运行

```sh
sd730-scheduler --nn-test
```

输出示例：

```
=== NN Model Test ===
[1] Model file: /data/adb/modules/sd730-scheduler/model/mlp_weights.txt
    Header: 10 16 2 (expect: 10 16 2)
    ✓ architecture OK
    Lines: 29 (expect 29 = 1+10+1+16+1)
    ✓ layout OK
[2] Live inference:
    output: 1.4210 0.6000
    ✓ k in [0.5, 2.0]
    ✓ cap in [0.5, 1.5]

✓✓✓ MODEL TEST PASSED - inference pipeline healthy
```

- 模型缺失 → 提示 `--nn-train`
- k/cap 越界 → 判定失败并提示重训（模型文件可能损坏）
- 可配合 `--nn-infer` 多次采样对比输出稳定性

---

## 五-b、训练数据量估算与抽样策略（v2.1.7）

**数据量估算**（采集周期约 1.2s/条，仅前台有 app 时采集）：

| 使用强度 | 每天条数 | 5 天原始数据 | 训练实际用（抽样上限）|
|---|---|---|---|
| 轻度（4h/天）| ~12,000 条 | ~60,000 条 | 20,000 条 |
| 普通（6h/天）| ~18,000 条 | ~90,000 条 | 20,000 条 |
| 中度（8h/天）| ~24,000 条 | ~120,000 条 | 20,000 条 |
| 重度（10h/天）| ~30,000 条 | ~150,000 条 | 20,000 条 |

模型 210 参数，需求经验值 2100~6300 条；2 万条上限远超需求，5 天跨度覆盖工作日+周末。

**时间衰减分层抽样（v2.1.7）**：
- 问题：均匀抽样时若最近 1-2 天数据量就攒够 2 万条，前面几天的数据会被完全抽不到（模型"失忆"）
- 方案：按天配额，每天至少保留代表性样本；权重随天数轻微衰减（最近 1.0，每往前一天 ×0.9）
- 效果：5 天数据量 [2k, 2k, 2k, 24k, 40k] 时，前 3 天保留率从均匀抽样的 8% 提升到 30%（老数据不丢，近期略多）

## 六、数据采集

```sh
# 启动采集器（常驻 1s 采样）
sh /data/adb/modules/sd730-scheduler/bin/collector.sh &

# 验证
wc -l /data/adb/modules/sd730-scheduler/data/collector/$(date +%Y%m%d).raw
cat /data/adb/modules/sd730-scheduler/data/collector/.coll_state   # 6字段快照
```

> **v3.1 起采集器开机自动启动，无需手动执行！** 由以下三处兜底拉起
> （`bin/ensure_collector.sh`，幂等，已运行则跳过）：
> 1. `post-fs-data.sh` 最前面（early 阶段）
> 2. `service.sh` 最前面 + 主循环 30 秒周期 watchdog
> 3. `common/functions.sh` 被 source 时的自愈钩子（覆盖旧版 service.sh）
>
> 手动启动仅用于调试。排查时看采集器日志：
> `cat /data/local/tmp/sd730-collector.log`（含 `ensure: stage=xxx starting collector` 记录）。
> `sd730-scheduler --nn-data` 可查看采集器运行状态。

保留天数：`config/nn.conf` 的 `nn_data_keep_days`（默认 5，范围 1-30），collector 每 1 秒轮询自动删除过期 `.raw`。

---

## 七、nn.conf 配置项

| 配置 | 默认 | 说明 |
|---|---|---|
| `nn_enabled` | `true` | 总开关（可用 --nn-enable/--nn-disable） |
| `nn_alpha` | `0.0` | 混合比例：0=纯规则，1=纯NN（建议 ≤0.5，可用 --nn-alpha 设置） |
| `nn_temp_limit` | `70` | 温度兜底（°C），超过强制回退纯规则 |
| `nn_data_keep_days` | `3` | 采集数据保留天数 |
| `nn_train_start_hour` / `nn_train_end_hour` | `1` / `4` | 训练窗口 |
| `nn_deep_standby_minutes` | `30` | 深度待机判定 |
| `nn_min_train_samples` | `500` | 训练最小样本数 |
| `nn_train_max_temp` | `55` | **训练温度上限（°C）**：训练前 + 训练中每 50 epoch 巡检，超限跳过/中止（v2.1.5） |
| `nn_warmup_seconds` / `nn_warmup_k_boost` | `3` / `0.3` | 前台预热 |

### 训练安全门控（v2.1.5 新增）

训练不再无条件执行，两道安全门：

```sh
# 门 1: 深度待机 (自动训练必须满足) —— 充电中 + 息屏 + 无前台
#       不满足时跳过训练, 避免边用边训/耗电发热
sd730-scheduler --nn-train          # 非深度待机 -> 跳过
sd730-scheduler --nn-train-now      # --force 强制, 跳过此门

# 门 2: 温度上限 (nn_train_max_temp, 默认 55°C)
#       非强制: 训练前检测 + 训练中每 50 epoch 巡检, 超限立即停止且保留旧模型
#       强制(--force): 超限仅警告继续; 绝对超过 85°C 才中止 (防物理损坏)
# 可调: config/nn.conf 的 nn_train_max_temp (30-90°C)
```

温度传感器读取 (v3.1.1)：只统计真正的温度传感器 zone，自动适配不同单位，并过滤
`lmh-dcvs`/`ibat`/`vbat`/`bcl`/`soc`/`*-step`/`*-lowf` 等非温度节点（旧版会把
`lmh-dcvs` 恒报的 75000 误判成 75°C 而错误阻断训练）。传感器全部不可用时不阻断训练；
超限时输出明确原因并退出码 1。

---

## 八、测试套件

```sh
cd /data/adb/modules/sd730-scheduler/test
python3 test_nn_scheduler.py       # v1.6 回归 60 用例
python3 test_model_learning.py     # 模型学习能力
python3 test_v2_1_pipeline.py      # 端到端管线 33 项
python3 test_pure_training.py      # 纯 Python 引擎 17 项
sh test_collector_sim.sh           # 采集器模拟 7 项
sh simulate_env_test.sh            # 真机环境模拟 15 项
```

---

## 九、上线流程（推荐）

```sh
# 1. 初始: nn_alpha=0.0 纯规则, 采集 5 天以上
# 2. 查看学习状态
sd730-scheduler --nn-status
# 3. 数据够了就手动训练
sd730-scheduler --nn-train
# 4. 测试模型
sd730-scheduler --nn-test
# 5. 逐步调混合比例 (每档观察 1-2 天)
sd730-scheduler --nn-alpha 0.1
sd730-scheduler --nn-alpha 0.2
sd730-scheduler --nn-alpha 0.3
# 6. 随时回退
sd730-scheduler --nn-alpha 0.0
```

---

## 十、故障排查

| 现象 | 原因 | 处理 |
|---|---|---|
| `--nn-status` 显示模型 MISSING | 未训练 | `sd730-scheduler --nn-train` |
| `--nn-test` 判定失败 | 模型损坏/越界 | 重训；或确认模型文件 29 行 |
| 训练报 numpy ImportError | 无 numpy | 自动切纯 Python 引擎；或 pip 装 numpy |
| `--nn-train` 无反应 | 样本 <500 或 Python 缺失 | `--nn-status` 看 Training readiness |
| `.raw` 不增长 | 采集器未运行/锁屏 | `sh bin/collector.sh &`，亮屏使用 |
| `--nn-infer` 输出 `1.0 1.0` | 模型缺失/损坏 | 重训 |
| 温度高但 k 仍大 | alpha 未生效 | 确认 nn_alpha>0 且 temp<=70 |

